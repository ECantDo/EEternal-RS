[package]
name = "eeternal_rs"
version = "0.0.2"
edition = "2024"
authors = ["ECanDo"]

[dependencies]
rand = "0.10.2"

[build]
rustflags = ["-C", "target-cpu=native"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"