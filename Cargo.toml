[package]
name = "eeternal_rs"
version = "0.0.3"
edition = "2024"
authors = ["ECanDo"]

[dependencies]

[build]
rustflags = ["-C", "target-cpu=native"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
#debug-assertions = true
#overflow-checks = true